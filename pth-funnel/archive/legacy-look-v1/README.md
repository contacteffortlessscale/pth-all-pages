# Legacy-look v1 (archived)

These four pages were re-skinned in the legacy GHL look (BaseLayout, Inter,
pth-purple header, pth-blue/pth-orange accents, emoji checkmarks). They were
the second iteration before we switched to embedding the original HTML
captured from theparttimehypnotist.com.

To restore: copy these back to `src/pages/` (overwriting the embed-style versions).
