# Design System v1 (archived)

These four pages were built in the PTH Design System light theme (dark text
on light surfaces, Oswald display, Atkinson body, cyan→blue→violet accents).

They've been swapped out temporarily for legacy-styled versions that match
the original theparttimehypnotist.com / GHL look (Inter, pth-purple header,
pth-blue + pth-orange accents, emoji checkmarks).

To restore: copy these files back to `src/pages/` overwriting the legacy
versions, and confirm the `FunnelLayout.astro` mode prop still works.
