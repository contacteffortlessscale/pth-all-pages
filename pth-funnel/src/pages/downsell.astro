---
// /downsell — currently disabled. Redirect to /thank-you, preserving the
// order token so the confirmation page still works.
const orderToken = Astro.url.searchParams.get('order') || '';
return Astro.redirect(orderToken ? `/thank-you?order=${encodeURIComponent(orderToken)}` : '/thank-you');
---
